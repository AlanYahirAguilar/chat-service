# whatsapp-service — microservicio TCP interno (sin dominio público)

# ── Requeridas ──
NODE_ENV=production
HOST=::
PORT=4011
# Carpeta de sesión Baileys — montar un VOLUMEN de Railway en /data
WHATSAPP_AUTH_DIR=/data/baileys
